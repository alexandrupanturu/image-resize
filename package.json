{
  "name": "image-resize",
  "version": "1.0.0",
  "description": "REST API to resize images",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "clean": "rm -rf build node_modules",
    "start": "npm run tsc && node ./build/server.js",
    "dev": "nodemon --ext ts --watch src --exec './node_modules/.bin/ts-node' src/server.ts"
  },
  "keywords": [],
  "author": "Alexandru Panturu",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.0.2",
    "dotenv": "^8.0.0",
    "express": "^4.16.4",
    "nodemon": "^1.19.0",
    "sharp": "^0.22.1",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  }
}
